//buttons
var searchButton = document.querySelector(".search-button");
var saveButton = document.querySelector(".save-button");
var showStarButton = document.querySelector(".starred-button");

//input forms
var titleForm = document.querySelector("#idea-title");
var bodyForm = document.querySelector("textarea");
var searchIdeasForm = document.querySelector("#search-box")

//


//need to create buttons, querySelector, and eventListeners for:
//star button, comment plus sign, x button

saveToStorage() {
  //if you click on the save button, add the Idea instance to the saved
  //ideas array.
  //require the title and body have values in them, else do not save.
  //update the inner HTML to add saved card to notes-container
  //when user clicks 'save', page does not reload, and title/body clear to default.
}

deleteFromStorage() {
    //if you click on the 'x' button, remove the Idea instance from the saved
    //ideas array
}
